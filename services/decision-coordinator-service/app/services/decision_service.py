def handle_decision_request(user_id: str, document_id: str):
    # Placeholder logic — to be implemented later
    print(f"Processing decision for user: {user_id} and document: {document_id}")
    
    return {
        "status": "received",
        "user_id": user_id,
        "document_id": document_id
    }